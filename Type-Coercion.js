//type coercion -- both items on sides on '==' are different types
//javascript will choose 1 type to covert both as type conversion
// be explicit to tell for type coercion


1 == '1'

if (1) {
    console.log(5)
}
