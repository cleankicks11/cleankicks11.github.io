
//Compound data types - combined primitive data types in useful ways

//empty arry - no items - length zero
const nums = [1,2,3];

console.log(nums.length);
console.log(nums[0]);

//pop() will cut off last item in array
const nums.pop();

//edit array iwht array index and new item

nums[0] = 0;

//push to add to end the of the array
nums.push(4);

console.log(nums);
